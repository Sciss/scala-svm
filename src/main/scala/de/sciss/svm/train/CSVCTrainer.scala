package de.sciss.svm
package train

private[svm] object CSVCTrainer extends Trainer {
  def train(param: SVMParameter, problem: Problem): Model = {

    ???
  }

  protected[this] def solver = ??? : FormulationSolver
}
